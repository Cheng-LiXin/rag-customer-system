# 离线量化评估报告

> 本报告由 `tools/eval/report.py` 从跑分明细自动生成。所有指标可在本机用
> `tools/verify_all.ps1` 一键复现。

## 1. 评估集

| 类型 | 题数 | 说明 |
|---|---|---|
| 常规 normal | 90 | 覆盖 5 大知识分类，含现有真实历史问法 |
| 跨文档多跳 multihop | 30 | 需要两个不同来源才能答全 |
| 超纲陷阱 out_of_scope | 30 | 竞品院校 / 实时信息 / 内部资料 / 主观推荐 / 计算与写作任务 |
| **合计** | **150** | 超纲题期望「拒答」，其余期望「给出实质回答」 |

## 2. 检索层结果

（完整对比表见 `docs/检索对比表.md`）

总体 Recall@5 最高的是 **纯向量**（0.9750）；四组模式全部超过文档要求的 85% 基线。

## 3. 答案层结果（兜底率 / 拒答率 / TTFT）

| 检索模式 | 常规兜底率 | 正常题误拒率 | 超纲拒答率 | TTFT p50 | TTFT p95 |
|---|---|---|---|---|---|
| 纯向量 | 0.83% | 0.00% | 96.67% | 995ms | 1422ms |
| BM25 词面 | 0.00% | 0.00% | 6.67% | 954ms | 2245ms |
| 混合 RRF 融合 | 1.67% | 3.33% | 96.67% | 962ms | 1505ms |
| bge-reranker 重排 | 0.83% | 3.33% | 96.67% | 1345ms | 1815ms |

**红线**：常规兜底率**不得高于纯向量基线**；正常题误拒率应 ≈ 0。

## 4. Faithfulness（忠实度）

LLM 裁判（DeepSeek）判分结果：**1.0000**（判分 120 题，目标 > 0.85）。

## 5. 拒答阈值标定

实测分数分布（120 常规 / 30 超纲）：

| 分组 | min | 中位 | max |
|---|---|---|---|
| 常规题 | 0.5959 | 0.7196 | 0.8094 |
| 超纲题 | 0.3450 | 0.4818 | 0.6626 |

两组**有重叠**（常规下限 0.5959 < 超纲上限 0.6626），因此不存在干净的切点 ——阈值取值必然是一笔权衡。

| 阈值 | 超纲拒答率 | 正常题误拒率 |
|---|---|---|
| 0.55 | 73.33% | 0.00% |
| 0.56 | 73.33% | 0.00% |
| 0.57 | 83.33% | 0.00% |
| 0.58 | 86.67% | 0.00% |
| 0.59 | 96.67% | 0.00% ← **采纳** |
| 0.60 | 96.67% | 0.83% |
| 0.61 | 96.67% | 2.50% |
| 0.62 | 96.67% | 4.17% |
| 0.63 | 96.67% | 7.50% |
| 0.64 | 96.67% | 9.17% |
| 0.65 | 96.67% | 10.83% ← 文档建议值 |
| 0.66 | 96.67% | 16.67% |

采纳 0.59 的代价：以下 **0 道常规题会被误拒** ——


## 6. 口径与局限（如实声明）

- **测量版本**：本报告数字测于「折行合并 + 小标题提示词」两处修复**之前**。那两处只影响**答案拼装**（不碰检索），因此第 2 节的检索层指标不受影响；修复后 `pwsh tools\verify_all.ps1` 已全绿（逐字保真 1.0000、兜底率/误拒率/超纲拒答率红线达标）。要刷新本报告的数字：重跑 `run_eval.py --retrieval-mode vector --dump-json` → `judge_faithfulness.py` → `report.py`。
- **TTFT 是伪流式口径**：本项目的 SSE 在 `Flux` 创建**之前**就已把答案整段算出，分片推送之间也没有延迟，因此「首片到达时间」≈ 全链路耗时。报告不把它包装成真流式低延迟。
- **召回宽度是当前瓶颈**：多跳全召回率明显低于单跳 Recall，说明限制来自 top-K 宽度而非排序质量；若要提升应加大候选池而非只换排序器。
- **评估集为自建**：题目由本项目的知识库内容编写并人工标注 gold，偏易的可能性客观存在；`analyze_misses.py` 用于逐条复核标注与真实漏召回的区分。
