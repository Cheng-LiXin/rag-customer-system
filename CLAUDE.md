# CLAUDE.md

基于 **RAG 的智能客服系统**（燕山大学毕业设计场景）。后端 Spring Boot 3 + 前端 Vue 3。技术栈、快速开始、接口清单、缓存策略见 `README.md`。

## 关键决策

- **版本锁定**：Spring AI 固定 `0.8.1`；JDK 17 + Maven；MyBatis-Plus `3.5.5`（逻辑删除 `@TableLogic`、`LambdaQueryWrapper`、`map-underscore-to-camel-case`）。
- **AI 模型**：Embedding 用 SiliconFlow `BAAI/bge-m3`（1024 维，`pgvector.dimensions=1024`）；Chat 用 `deepseek-chat`。**DeepSeek 官方无 Embedding 接口**。
- **Spring AI `Document`** 用三参构造 `Document(id, content, metadata)` 设向量 ID，无 builder。
- **默认账号**：admin / agent / user，密码 **`Admin@Ysu2026` / `Agent@Ysu2026` / `User@Ysu2026`**（可用 `DEMO_ADMIN_PASSWORD` 等环境变量覆盖）。角色返回带 `ROLE_` 前缀。重建后 id = **130001 / 120001 / 110001**（6 位账号号，见下「账号体系」）。**密码重置由 `rag.demo.reset-passwords` 控制**：本地开发默认 true（改坏了重启即恢复）；**docker/部署 profile 默认 false** —— 否则后台改过的口令会在重启时被改回默认值。⚠ 这三个口令写在仓库里，真上线前必须换掉（环境变量覆盖 + 应用内改密）。
- **账号体系（6 位角色前缀账号号 + 自助注册 + 昵称限改 + 违禁词）**：`sys_user.id` = 前两位前缀（**11 用户 / 12 客服 / 13 管理员**）×10000 + 同前缀 4 位序号；`@TableId(type = IdType.INPUT)`，出号**统一走 `AccountNoService.nextId(prefix)`**（`@Transactional` 内 `IdSeqMapper` 对 `sys_id_seq` 行 `UPDATE curr=curr+1` 行锁分配，先 bump 后读，返回 `prefix*10000L+curr`），建号入口不留手写字面量：`DataInitializer`（demo 首启 110001/120001/130001）、`AuthController.register`（固定 `PREFIX_USER`=11）、`SysUserController.create`（按 roleIds 中**最高角色**定前缀：含 role1→13、否则含 role2→12、否则 11；id 只反映建号时最高角色，后续改角色不重写）。**自助注册**：`POST /api/auth/register`（SecurityConfig permitAll）手机号作账号（写现有 `username` 列 + `phone` 列，暂不接短信验证码），BCrypt 加密，`RegisterRequest` 带 `confirmPassword` 二次校验，昵称可选（空则默认「用户+手机尾号4位」且**不启动**限改计时），重复注册报「该手机号已注册」。**昵称 30 天限改**：`sys_user.nickname_updated_at` 记录最近改昵称时间；`AuthController.updateProfile` 仅 nickname 变化时校验（admin `SysUserController.update` 改他人昵称**不受限且不动时间戳**）；`profile()` 返回 `nicknameUpdatedAt/nicknameEditable/nicknameCooldownDays` 供前端禁用 + 倒计时。**违禁词屏蔽（资料提交，不拦聊天）**：`sys_banned_word(id, word UNIQUE, status)`；`BannedWordService.findBanned(text)` 取 status=1 词表做 substring 命中；在 register（昵称）、updateProfile（昵称/邮箱）、SysUserController.create/update（昵称）处强制拦截并给友好文案；admin CRUD 走 `BannedWordController`（`/api/admin/banned-word`，类级 `@PreAuthorize("hasRole('ADMIN')")`），前端 `admin/BannedWord.vue`。历史测试数据于 `tools/db/migration_account_v2.sql`（一次性）清空重建；**铁律：保留 guest=0 哨兵、id 保持纯数字、新用户一律走 `AccountNoService`、不在别处对 id 做前缀解析**（id 全链路按不透明 Long 用，经审计安全）。
- **SSE** 返回 `Flux<String>`（非 `ServerSentEvent`），前端解析需兼容 `data:` 前缀与裸 JSON 两种分行。
- **RAG 缓存**：键 `qa:cache:{MD5(问题)}`，TTL 默认 `3600s`；命中直接返回，未命中异步写回。
- **上手问答兜底（你好/你是谁/你可以做什么/怎么使用，防泛问句混入无关资料）**：`ChatServiceImpl` 在 `ask|stream` 的 qa:cache 命中检查之后先跑确定性 `faqAnswer(raw)` 短路：用 `FAQ_PUNCT=[\p{P}\p{S}\s]` 剥掉标点空白得核心串，空或长度>12 即 null 走正常 RAG；问候类（你好/您好/哈喽/hello/hi/嗨/早上好/晚上好 或「在吗」）回固定 `FAQ_GREET`，含「你是谁/介绍/机器人」回 `FAQ_WHO`，含「做什么/功能/能回答」回 `FAQ_CAN`，`FAQ_HOW_PHRASES` 短语集（怎么用/怎么使用/如何使用/怎么操作/怎么提问/快速开始/使用说明…）命中回 `FAQ_HOW`——均带换行 markdown、**不带尾注、不入 qa:cache**。根因：泛问句会召回到不相干知识（实测「你可以做什么」混入浴池圈存/班车预约噪声）。**知识库侧同步入库**：新分类 5「智能客服助手」，4 条 onboarding 片段存 `tools/kb-import/onboarding_assistant.csv`（你好/你是谁/你可以做什么/怎么使用，来源链接留空）。短路只拦短精确短语——真实问题（「怎么使用校园卡充值」）不命中短语集仍走 RAG（实测未被劫持）。
- **问答生成（关键，防语序错乱）**：`ChatServiceImpl` 用**抽取式回答**——把命中片段按「换行 + 句末标点（。！？!?）」切成句元编号，让模型只输出能回答的**句子编号**（不造句不复述），Java 侧按编号把**原文句元原样拼接**为答案；编号选空时先收敛到 top-1 片段再选一次，仍空则回固定文案「资料中没有相关内容，建议换个问法或补充关键词」——**从不把整段长文交给模型复述**（该路径是乱序根源，已删除 `generate`/`buildPrompt` 兜底）。`ask|send|stream` 共用主路径；SSE 的 token 事件是把最终答案约 18 字符分片伪造的流式。
- **结构化 markdown 回答（默认开）**：`answer()` 分 A/B 两路。**A 路**（`rag.structured.enabled:true`）一次调用让模型只输出分组 JSON `{"groups":[{"t":"小标题","n":[原文行号…]}]}`（不重写任何事实句），Java 按行号逐字拼原文渲染：每组 = `**小标题**` 独立行，其下**每条原文句渲染为一条 `- ` 列表项**（逐字保真、条例清晰）；竖线表格行整行作一个句元、聚成合法 markdown 表格（首行表头 + 自动补 `|---|` 分隔行）；某句含「见下表/如下/如表」且其下同片段紧邻表格时把整块并入该组（杜绝「具体见下表」悬空）；展示层剥原文序号前缀（`1.`/`三．`）。**参考文件尾注**：`answer()` 成功路径经 `withFooter`→`referenceFooter` 按 chunk_id 回查 `knowledge_chunk` 的 `source_url/source_title`，在答案末尾附「参考文件：」列表（**按文件去重**、标题取干净 `source_title`、带链接）——仿学校官方机器人；否定兜底/空答**不带尾注**。**B 路** = 旧版扁平抽取式三档**逐字节保留**：A 路异常/输出为空/`enabled:false` 时降级到 B，兜底文案出现频率**绝不高于旧版**；否定兜底仍不入 `qa:cache`。句元切分：竖线表格行先判整行原子，再按句末标点切（`UnitItem{docIndex,text,tableLine}`）。**参考来源**（`toSources`）完整返回片段（`tableize` 把竖线表转 markdown，留作未来「展开查看」）；但前端 `ChatHome.vue` 参考来源**只展示标题 + 分类 + 相似度，不渲染正文**。配置：`rag.structured.max-groups:4 / max-per-group:4`（答案要点数、每要点最多引用行数）。实测：问「2026 本科新生入学须知」答出报到时间 + 真表格 + 证件/接站/户口等分点，末尾附入学须知官网链接；问「8月27日报到学院」直接给表。
- **命中率（防“没有内容”误判，ChatServiceImpl.answer 三档）**：绝大多数命中率问题不是“没召回”而是“门槛过严”——库里是具体问答、用户问得宽泛时严格选句（只挑“能直接回答”的单句）找不到句子就误回兜底。修法：①严格选句（全量）→②严格选句（收敛 top-1）后仍“有效为空”时，若最相似片段相似度 ≥ `rag.partial-min-score`(默认 0.55) 追加 **③宽松档** `selectPartial`——让模型在候选里挑**承载实质信息、至多 3 句**（仍只输出编号、Java 拼原文、不造句，`isQuestionUnit` 把以 `?` 结尾的纯提问/标题句剔出答案、`isEmptySelection` 判定“只选了提问句=没答上”）。验证：`燕山大学的招生政策是什么`(宽泛) 此前兜底 → 现答出卓越计划等 3 句；`学校有哪些特色专业` 此前只回标题句 → 现答出重点学科/一流专业清单。**兜底文案「没有内容」不再写入 `qa:cache:*`**（见踩坑：旧否定缓存会令复测误命中）。
- **意图识别（评分式规则，F03，防“其他”泛滥）**：`IntentService.classify` 从「首个命中即返回」升级为**评分式**——扫描整张词表，每个命中关键词给其所属意图 +1 分；取分最高者；平票再比「该意图命中最长关键词的长度」（专词压泛词：「一卡通」>「校园」、「转专业」>「专业」）；仍并列按规范顺序 `INTENT_ORDER`（招生政策/教务服务/学工服务/校园生活/专业设置/学校概况）取先。词表 ~110 词分 6 类（招生政策/教务服务/学工服务/学校概况对齐知识分类，另加专业设置/校园生活两个高频运营主题），其余落「其他」。持久化于每条 USER 消息 `message.intent_category`（`ChatServiceImpl.persistExchange` 与 `buildAnswer` 两处）、`StatisticsMapper.intentDist` 聚合饼图、机器人气泡回显「意图：XX」。**历史回填**：`POST /api/intent/reclassify`（ADMIN——用方法级 `@PreAuthorize`，因 `/api/intent/**` 在 URL 层 permitAll）按新词表重算存量 USER 消息、只更新变化行、返回 `{scanned,updated}`。规则映射回归锁在 `IntentServiceTest`（纯 JUnit、无 Spring 上下文，8 用例覆盖六类真实问法/重叠词/兜底）。
- **统计 / 日志**：ECharts `5.5.1`；操作日志用 Spring AOP（`@OperationLog` 注解 + `OperationLogAspect`，拦截写接口记用户/模块/动作/路径/IP/参数/耗时/成败）。热门知识 Top10（`StatisticsMapper.hotKnowledge`）按**源文**聚合：`GROUP BY COALESCE(NULLIF(source_url,''), MD5(COALESCE(NULLIF(source_title,''), title)))`，SUM(hit_count) 合并同一篇被切成多片的命中、chunkCount=几片，标题取 `MAX(COALESCE(source_title,title))`——**导入时必须让 source_url 与 source_title 非空**（否则退化为按标题归并）。
- **向量化只用 `content`，标题不进向量**：`KnowledgeServiceImpl.embedAndStore` 里 `new Document(vectorId, chunk.getContent(), metadata)`，`title` 只放 metadata 供展示/`Source.title` 引用。因此**改标题（如加小标题）不影响检索命中率，也不影响抽取式回答的语序**——标题纯属展示层与统计层字段。`knowledge_chunk` 表 `title` =「小标题——源标题」，另加 `source_title` 列存干净源标题（热门 Top10 用它取标题，避免被小标题前缀污染）。
- **管理端文档上传导入（预览确认，2026-09-09）**：`/admin/knowledge` 工具栏新增「上传文档」，支持 `.txt` / `.md` / `.doc` / `.docx` / `.pdf`。**无状态两段式**：`POST /api/knowledge/document/parse`（multipart `file`，只解析+切块返回预览、**不落库**）→ 前端预览弹窗（分类**必填**、源标题默认取文件名、来源链接可选、切块默认全选可勾选）→ `POST /api/knowledge/document/import`（JSON，落库 + 批量向量化）。**解析** `DocumentParser`：txt/md 用 UTF-8 严格解码失败回退 GB18030（对齐离线脚本）并剥 BOM；docx 用 **Apache POI `XWPFDocument`**（依赖 `poi-ooxml:5.2.5`），样式名 `Heading N`/`标题 N` 映射为 `#` 前缀、表格行拼 ` | `、**每段后补空行**（Word 通常不用空行分段，不补则整篇会并成一条）；`.doc`（老式二进制）用 **POI `HWPFDocument`+`WordExtractor`**（依赖 `poi-scratchpad`，样式信息难映射标题故退化为段落文本，**开头 `{\rtf` 的伪 .doc 先拦并提示改存**）；`.pdf` 用 **Apache PDFBox `PDFTextStripper`(sortByPosition)**（依赖 `pdfbox:2.0.31`），加密/扫描件（非空白字符<20）友好报错，并对 PDF 文本做 CJK 相邻空格规整（删除按位置重排在中文之间插入的孤立空格，提升 embedding）。**切块** `DocumentChunker` 移植 `tools/kb-import/kb_csv_builder.ps1` 规则：`#`~`######` 为块边界、空行拆段、噪音行忽略（图片/分隔线/HTML 注释/代码围栏）、贪心聚合 **150~500 字**、content 忽略大小写去重、<20 字丢弃、标题清洗（去加粗/行内标记）截断 200。**入库**写 `sourceType="DOC"` + `chunkIndex` 递增（`addChunk` 抽出 `insertChunk(request, sourceType, chunkIndex)` 供两条路径共用）；**批量向量化**（一次 `vectorStore.add(List)`，几十条逐条调会逼近前端 60s 超时），返回 `{success, fail, vectorFail, errors}` —— **`vectorFail` 必须单独统计**，因为 `embedAndStore` 吞异常只置 `vectorStatus=2`，`success` 仅代表 MySQL 写入成功。实测：docx（含 H1/H2 与中文正文）解析出 4 块、标题映射正确、噪声行被滤；导入 3 块后 `sourceType=DOC`/`chunkIndex` 0-2/`vectorStatus=1`，再问「选课分几个阶段」命中新片段（0.74/0.73）且「参考文件」自动带 sourceUrl。另实测：Word 生成的真实中文 `.doc` 与 `.pdf` 各解析 2 块、`.doc` 导入后问「国家奖学金奖励标准」命中（0.78/0.64）。**预览标题/正文可编辑（2026-09-09 二期）**：parse 返回前由 `SegmentTitleGenerator` 对每个切块用 DeepSeek 生成 **6~14 字小标题**（`new Prompt(指令+内容前900字)` 与 ChatServiceImpl 同款 ChatClient；**按内容 MD5 进程内缓存**、3 线程并发、失败回退切块原标题/段首短句）——预览里标题不再是文件名；前端弹窗的「标题」列改**可编辑 el-input**、「内容」列可**点开全文**在独立弹窗编辑（删除页眉页脚/落款等噪音后保存，字数/总数实时重算），确认导入用改后的 title/content（标题留空由后端 `resolveTitle` 兜底）。实测：2 块文档首次解析（含 2 次 LLM 标题）约 0.9s，二次命中缓存 0.1s。
- **会话生命周期（复用 + 超时自动结束）**：`ChatServiceImpl.ensureConversation` 复用「进行中(status=1)」会话——登录用户按 `userId` 复用最近一条 AUTO 会话，按 `conversationId` 复用须 `status=1` 且归属一致（`sameUser`，防换账号串号）；结束/非本人则新开会话。`ConversationAutoCloseTask`（`@Scheduled`，`SchedulingConfig` 开启 `@EnableScheduling`）定时把「连续 `rag.session.timeout-minutes`(30) 分钟无消息」的 AUTO 会话置 `status=2`，活跃判据用 `MAX(message.create_time)`（`ConversationMapper.closeStaleAuto`）。**人工会话也超时自动关**：`rag.human.timeout-minutes`(默认 10) 起，同任务 `closeStaleHumanConversations` 查「已分配客服(agent_id 非空)且双方连续超时无对话」的 HUMAN 会话（`ConversationMapper.selectStaleHuman`，无任何消息时退化用 `conversation.create_time`），逐个调 `AgentService.closeConversation(id,"timeout")` 走**完整收尾**（置 status=2、从 Redis 排队/分配/负载摘除、WS 通知客服与用户）；排队未接（agent_id 空）的人工会话不自动关，避免误杀仍在等待的用户。`AgentService.closeConversation` 现为重载：无参版本=客服/用户主动结束(reason=manual)，带参版本多一个 reason；WS `closed` 事件携带 `reason`(manual/timeout)，前端据此区分文案（「客服已结束本次会话」 vs 「长时间未对话，会话已自动结束」）。转人工会把原 AI 会话 `session_type` 升级为 HUMAN。前端 `ChatHome.vue` 把 `conversationId` 存 `localStorage`（刷新/回首页不丢），登出时清除。
- **管理后台会话管理**：`ConversationController.page` 支持 `agentId` 筛选参数；前端 `admin/Conversation.vue` 新增「处理客服」列（显示 agentId），消息弹窗中客服消息(AGENT)高亮显示。
- **工单管理（客服端落地）**：`TicketController.create` 客服创建工单时自动分配给自己（`assigneeId` = 当前用户，`status` 直接进入处理中）；`page` 接口新增 `assigneeId` 筛选参数。前端客服工作台 `Workbench.vue` 侧边栏新增「我的工单」区（`AgentTicket.vue` 组件）：筛选状态、新建工单（自动关联当前会话）、工单详情弹窗（状态流转：待处理→处理中→已解决→已关闭）；聊天窗口头部新增「创建工单」快捷按钮。
- **满意度评价防刷（同一会话至多一条，防重复无限堆积）**：`SatisfactionController.submit` 旧实现每次无条件 `insert`，可被对同一 `conversationId` 无限刷评价并污染 `StatisticsMapper.satisfactionStat`（`AVG(rating)/COUNT(*)`）与 `satisfactionCount`。改法：①先按 `conversation_id` 查已存在则**更新覆盖** rating/comment（再次评价=改评价而非加行）；②DB 唯一键 `uk_conversation(conversation_id)` 兜底并发首评（`DuplicateKeyException` 时回读再更新）；③`userId` 改由服务端 `SecurityUtil`+`UserService.getByUsername` 从 JWT 解析，不信任前端传值（本接口需 JWT 故总有当前用户）。迁移：`tools/db/migration_satisfaction_unique.sql`（先删每会话历史重复保留最新、幂等加唯一键），`schema.sql` 已同步。
- **已登录用户历史对话**：`ChatController` 新增 `GET /api/chat/history`（分页返回当前用户会话列表）和 `GET /api/chat/history/{conversationId}/messages`（返回指定会话消息，含归属校验）。前端 `ChatHome.vue` 用户下拉菜单新增「历史对话」，打开右侧抽屉（`el-drawer`）展示会话列表，点击会话加载消息到聊天区域继续查看。`/api/chat/**` 在 `permitAll`，未登录时 history 接口返回空列表。
- **客户资料（地区/身份）与客服名片**：`sys_user` 增列 `province`/`city`/`identity`（身份：考生/本科生/硕士生，字面量来自 `frontend/src/data/region.ts` 的 `identityOptions`）。`GET /auth/profile` 与 `PUT /auth/profile` 读写这些字段；`ProfileRequest`、`ProfileResult`、`Profile.vue` 相应扩展，个人中心「所在地区」用 `region.ts`（34 省级 + 下属市）做省-市两个级联下拉（`regions`，改省清空已选市）。
- **昵称同步（关键，勿存冗余昵称）**：会话列表里的归属用户名一律**实时 join**：管理端 `ConversationController`、客服端 `AgentService` 都注入 `SysUserMapper`，用 `selectBatchIds` 给 transient `@TableField(exist=false) userName` 回填（`nickname` || `username`；userId=0/null 游客留空，前端按 `convLabel` 兜底「游客」）。这样用户改昵称后客服/管理后台立即同步，无需同步写冗余列。客服端还会把 `userName` 放进 `toConversationMap`（Socket 负载）。
- **客服名片接口**：`GET /api/conversation/{id}/customer` 返回会话归属客户的完整资料（userId/username/nickname/avatar/email/phone/province/city/identity/status/createTime；userId 为 0/null 时返回 `{guest:true,userId:0}`）。`Workbench.vue` 右侧 `aside.wb-profile` 客户名片卡展示头像/昵称/@用户名/身份标签/账号状态/地区/邮箱/手机号/账号ID/注册时间，游客会话显示游客兜底；当前活动会话收到新消息时自动刷新名片。
- **个人信息页 / 会话归属展示**：`/profile` 为独立全屏页（自带顶栏返回、身份卡[头像/昵称/角色/注册日/账号状态/历史对话数]、基础资料 + 账号安全两栏）。`/auth/profile` 补充返回 `status`。`Conversation` 实体带 `@TableField(exist=false) userName`（非 DB 列），`ConversationController.page` 用 `sys_user.selectBatchIds` 回填昵称/用户名；`userId=0` 前端显示「游客」。

- **引用溯源（批次 A）**：答案正文每个原文句尾的行内标记 `[n]` 由 **Java 在拼装阶段按句元的 `docIndex` 追加**（`ChatServiceImpl.cite()`，n 指向 `sources[n-1]`），**绝不让模型输出引用号** —— 这是"抽取式逐字保真"铁律的必然推论。`Source` 带 `index/sourceUrl/sourceTitle/updateTime`；`toSources` 与 `referenceFooter` 共用 `loadChunks()`（原先各查一遍库）。前端 `MarkdownText.vue` 用 marked **inline 扩展**把 `[n]` 渲染成可点 `sup.cite`（不能渲染完 HTML 再正则替换，会误伤代码块），`ChatHome.vue` 点击展开原条目并滚动定位。开关 `rag.citation.inline`（关掉即与旧版逐字节一致）。
- **拒答与低置信（批次 A/D）**：`ChatServiceImpl.rejectPolicy` 在**检索之后、调模型之前**判定：最高相似度 < `rag.answer.reject-threshold` → 直接拒答并引导转人工，**不调大模型**；落在 `[reject-threshold, low-confidence-threshold)` → 照常作答但标 `lowConfidence`。拒答与兜底**都不入 `qa:cache`**。默认阈值 **0.59**（非文档建议的 0.65）：150 题实测常规题相似度下限 0.5552、超纲题上限 0.6629，**两组有重叠、不存在干净切点**；0.55 时超纲拒答率仅 73.3% 不达标、0.65 达标但误拒 15/120，0.59 取到 96.7% + 误拒 4/120。阈值由 `tools/eval/threshold_sweep.py` **离线扫描**定稿（拒答是 `score < T` 的纯函数，不必改配置重启去试）。被误拒的 4 道题题号记在 `docs/评估报告.md`。
- **可插拔检索（批次 D）**：`com.rag.retrieve` 包 —— `Retriever` 接口 + `VectorRetriever/Bm25Retriever/HybridRrfRetriever/RerankRetriever` + `RetrieverFactory`（优先级：请求头 `X-Retrieval-Mode` → Redis `rag:retrieve:mode` → 配置 `rag.retrieve.mode`，未知模式回落 vector）。**核心不变量**：`RetrievalResult.maxVectorSimilarity` 固定取「**融合前**向量宽池 top-1 余弦」，拒答阈值与 `partial-min-score` 只读它 —— 保证四种模式下阈值语义一致，且 `mode=vector` 与改造前逐字节等价。BM25 用**字符 2-gram + 数字/英文整体保留**（不引 jieba/lucene，零依赖可复现；数字被 bigram 拆成 20/02/26 会糊掉）；RRF `k=60` 只用排名不用分数（BM25 分数无上界、跨 query 不可比）；reranker 走硅基流动 HTTP（复用 embedding 的 key，**超时/失败一律降级为原序**，绝不让重排失败导致问答失败）。⚠ **纯 BM25 模式下拒答阈值失效**（没有向量相似度可判）—— 实测超纲拒答率掉到 6.7%。
- **评估口径（批次 D，最容易搞错的一点）**：**Recall@K 必须在检索层量**（ADMIN 接口 `/api/knowledge/chunk/retrieve-preview`，`run_eval.py --retrieval`），不能从 `/api/chat/ask` 的 sources 推 —— 后者只有 `rag.top-k=3` 条，那算的是 Recall@3。答案路径的 top-k 刻意不提：为凑指标提高它会把噪声片段塞进抽取池、污染已调好的答案质量。多跳题另算 `full_recall`（**两个来源都进 top-K** 才算），只看"命中其一"会严重高估。缓存旁路走 `?noCache=true` + `RAG_EVAL_BYPASS`（**跑分前必须先过 `probe_env.py` 确认旁路真的生效**，否则重复问题命中缓存、指标全是残留值）。
- **注入防护三层（批次 B）**：`com.rag.guard` —— L1 输入层（检索前，扫用户问题）／L2 上下文层（检索后生成前，扫片段内容并记录被污染的 `chunk_id`）／L3 输出层（生成后，抽取式回答逐字引用原文，**事实式**污染承诺只能在这里兜住，与检索路径解耦）。规则一律「**意图词 + 对象词共现**」，**绝不单词匹配**（知识库正文里「保证」「全额」「打印」「录取」都是合法词）。运行时开关走 Redis `rag:guard:enabled`（管理端可切，切换动作另落 `operation_log`），命中落 `guard_event` 表。4 个提示词拼接点统一走 `PromptSanitizer` 包 `<question>`/`<content>` 标签。**不引入 LLM 二次判定**（同模型判同文本是已知弱点，且加时延与不确定性），留 `llm-second-opinion` 开关。演示与清理见 `docs/注入防护演示.md`。
- **数据飞轮（批次 E）**：`message` 增 `feedback/feedback_time/feedback_comment`；新表 `unresolved_question`（`question_hash` 唯一键把重复提问**收敛累加** `hit_count`，`source` = 1兜底/2点踩/3拒答/4防护拦截）。记录点：`ChatServiceImpl.recordUnresolved`（三个自动来源）+ `MessageFeedbackController`（点踩）。反馈接口放 **`/api/message/**`（走认证）而不是 `/api/chat/**`**（后者 permitAll，挂那儿就得自证归属且可匿名刷），代价是游客不能反馈、前端对未登录隐藏按钮。**缓存命中也落库**（`rag.cache.persist-on-hit`，默认开）：否则热门的重复问题在库里没有机器人消息、用户根本没法点赞 —— 对最热的问题反而失效。`ChatAnswer.messageId` 必须用**本次**新建的消息 id（缓存里那份是上一次的，直接用会让用户给别人的旧回答点赞）。
- **Docker 全栈一键起（批次 C）**：`docker-compose.yml` 用 **profile 隔离** —— `docker compose up -d` 只起依赖（本机开发的既有用法，行为不变），`docker compose --profile full up -d --build` 才起后端+前端+MySQL+PG+Redis（否则后端容器会和本机 8080 抢端口）。Web 入口 nginx 在 **8081**（避开 Windows 的 winnat 排除端口），MySQL 映射 **3307**（避本机 3306），后端**不映射到宿主机**。`frontend/nginx.conf` 里 **SSE 必须 `proxy_buffering off`**、`/ws/` 必须带 `Upgrade` 头。`application-docker.yml` 只改主机名与口令来源、**不含任何口令**。数据迁移走 `tools/docker/export-mysql.ps1`；初始化脚本目录见 `tools/docker/initdb/README.md`。⚠ **`docker compose down -v` 会连 pg-data 一起删**（向量全没、需重新向量化），只重建 MySQL 要用 `docker volume rm rag-customer-system_mysql-data`。
- **可观测性（批次 F）**：actuator + micrometer，指标端口 `management.server.port=9091` **不映射宿主机**，只给容器网络内的 Prometheus 抓。业务指标 `rag.qa.answers{result}`（结局分布：answered/fallback/refused/blocked/cached）、`rag.qa.ttft`、`rag.guard.blocks{layer,rule}`、`rag.corpus.size`。Grafana 数据源与看板走 provisioning 自动加载（`tools/monitoring/`）。**actuator 路径要在 SecurityConfig 里放行**，否则 Prometheus 拿到 401。

- **知识库去重（`tools/kb-dedupe/find_duplicates.py`）**：评估实测挖出「同一份文件的两个版本」——公文类知识库常见「通知 + 附件全文」的重复（实测：《学业预警工作实施细则》本体 149-153 与印发它的通知 222-226，正文一字不差）。判定必须用**文档级**而非片段级：只有当 A 文档的**大多数**片段都能在 B 里找到近重复时才判定同源。片段级包含度会大量误报（实测把「学科实力/ESI」vs「学位授权点」、「录取分数」vs「录取查询」都判成重复——同领域共享术语、短片段共享 URL 样板）。工具内含**评估集保护**：`golden_set.csv` 引用的 chunk 绝不会被删；删除走 `DELETE /api/knowledge/chunk/{id}`（连带删向量，直连库删行会留幽灵向量），且必须先 `--backup`。**删完必须重映射评估集里的 gold id**（实测影响 7 道题），再重跑 `verify_all.ps1`。

## 踩坑

- **列举型问法漏召回 = 内容缺实体锚点（数据策展，非检索 bug）**：问「燕山大学都有哪些学院」回兜底、但「学院设置」能答（2026-09-05 实测修复）。探针（SiliconFlow BAAI/bge-m3 嵌入问法，`docker exec rag-postgres psql` 直查 `vector_store` 按 `embedding<=>'[...]'::vector` 排序比对多问法）证明：含 19 学院清单的片段在「学院设置」下排 **#1**，但只要问句带「燕山大学」锚点就跌到 **#44~50**——招生问答等满篇「燕山大学+学科/专业」的噪声片段（0.62~0.66）把真片段淹没；真片段原文以「学校设有…」开头、**全篇缺字面「燕山大学」**（靠文档语境省略主语）。修法：混合片段**按话题拆纯** + 权威句补全称主语，如 `学院设置：燕山大学设有研究生院和19个直属学院，即…`，相似度 0.57→0.76 稳居 top-1（忠实改写，未虚构）。**数据策展纪律：权威句带全称主语；一个片段一个话题**（KB 校验「152 全向量就绪」时发现记忆旧计数「203」与库不符——历史重导留 56 条逻辑删除，活跃数以 `COUNT(*) WHERE deleted=0` 为准）。
- **注解与实体同名**（`com.rag.annotation.OperationLog` vs `com.rag.entity.OperationLog`）会触发 Lombok **级联报错**（大量 `cannot find symbol`），本质是单个 broken 文件。用**全限定名**规避。
- **前端 `counts` 重复声明** → `vite build` 报 `Identifier 'counts' has already been declared`；用解构重命名（如 `counts: hotCounts`）。
- **echarts 非预装**，需 `npm i echarts`；引入后 Statistics chunk 约 1MB，属正常。
- **转人工 / 满意度需 JWT**：`POST /api/agent/transfer` 与 `POST /api/satisfaction` 不在 `permitAll`，前端须登录后调用。
- **改 `schema.sql` 后**需重新 `mysql -uroot -p < src/main/resources/schema.sql`（DROP+CREATE 会清数据）；**增量加列用 `ALTER TABLE ...`** 避免清库。
- **CSV 导出带 `﻿` BOM**（Excel 兼容）；Java 源码出现 U+FEFF 合法可编译。
- **客服工作台**需先 `GET /api/auth/me` 拿客服数字 id，再用它查 Redis `rag:cs:assign`。
- **QA 旧否定缓存**：检索/抽取修好后再复测某句，若它在此前已被回“没有内容”且写入 `qa:cache:{MD5(问题)}`（TTL 1h），会**命中旧否定、绕过新逻辑**（实测重现：8081 新代码先复现旧答案，清 `qa:cache:*` 后即恢复正常）。修复后代码已改为否定答案不入缓存，但存量旧否定需清 Redis：`docker exec rag-redis redis-cli EVAL "local k=redis.call('keys','qa:cache:*'); if #k>0 then return redis.call('del',unpack(k)) else return 0 end" 0`。
- **词序错乱根因**：`deepseek-chat`（已路由到偏小的 fast 模型）对含表格/TOC 噪声的整段**长文本逐字复述不稳定**，`temperature=0` 也压不住（同一问题同一参数，前一次干净后一次乱）。对策见上方「问答生成」：只让模型选编号、Java 拼原文，不依赖其复述能力。
- **`/api/chat/ask|send` 报 500「系统繁忙/消息内容不能为空」**：非后端 bug，是请求体字段应为 `message`（`ChatRequest`），不是 `question`；SSE 用 `GET ?message=`。
- **SSE 把会话绑到 userId=0（游客）**：`/api/chat/stream` 返回 `Flux` 后事务在 **Reactor 线程**执行，`SecurityContextHolder`(ThreadLocal) 已不可达 → `ChatServiceImpl.resolveUserId()` 返回 0。修复：在 servlet 请求线程先 `resolveUserId()` 再把 userId 透传给 `persistExchange`/`ensureConversation`（不再在事务里解析），`ask` 同步路径同步改造为显式传参。连带好处：转人工时 `AgentService.resolveHumanConversation` 的 `isSameUser` 校验通过，可正常复用原 AI 会话而非另开新 HUMAN 会话。注：修复只作用于新会话，历史 userId=0 数据无法自动归属回用户。
- **注册/资料相关 500「系统繁忙」可能不是后端 bug，是请求编码问题**：Windows PowerShell 直接 `curl --data-raw '{"nickname":"垃圾"}'` 会把中文按 **GBK** 字节发送 → Jackson `Invalid UTF-8 start byte` → 业务异常被兜底成「系统繁忙」。正确冒烟姿势：用 `[System.IO.File]::WriteAllText(path, json, UTF8无BOM)` 落 UTF-8 文件再 `curl --data-binary @file`。另注意：这些接口的**业务错误（重复注册/违禁词/30 天限改）走 HTTP 200 + body `code:500`**（前端拦截器已统一 `ElMessage` 提示）。
- **改 `sys_user.id` 后**：JWT subject 仍是 **username**（手机号/账号名），登录期用户 id 一律经 `/auth/me` 或 DB 现查，故 6 位账号号对既有鉴权/会话/Redis 键透明；历史会话归属 userId=0 的数据不会自动回填。`DataInitializer` 每次启动重置 demo 密码但**不重建**已存在的 demo 行（迁移后再首启只补角色绑定）。
- **GFM 表格会丢弃超出表头列数的单元格** —— 表格的引用号 `[n]` 若直接接在最后一行末尾，会被当成多出来的第 N+1 列**静默吃掉**（不报错、渲染出来就是没有）。必须**另起一段**单独放。已留回归脚本 `frontend/scripts/check-citations.mjs`（`node scripts/check-citations.mjs`，7 项断言），这个 bug 就是它抓出来的。
- **本机 Docker Hub 直连不通**（`registry-1.docker.io` 超时），但 **DaoCloud 镜像源可达**。拉基础镜像的办法：`docker pull docker.m.daocloud.io/library/<name>:<tag>` 再 `docker tag` 回官方名 —— **不必改 daemon.json、不必重启 Docker**。注意该源有**白名单**，`justbamboo/jmeter`、`qainsights/jmeter` 这类小众镜像不在其中（JMeter 改为下载 Apache 官方 zip 到 `tools/loadtest/.jmeter/`，本机有 JDK 17 即可跑）。
- **Spring Boot 管理端口不自动放行 actuator**：`management.server.port=9091` 之后，`/actuator/prometheus` 仍走主过滤器链 → Prometheus 拿到 **401**。要在 `SecurityConfig` 的 permitAll 里显式加 `/actuator/health|info|prometheus`（该端口不映射宿主机，放行是安全的）。
- **SSE 是"传输层流式"而非"逐字流式"**：`ChatServiceImpl.stream()` 用 `Flux.fromIterable(slice(answer,18))` **没有 `delayElements`**，所以答案在 Flux 创建前已算完、分片之间也没有间隔 —— 客户端收到的是一串几乎同时到达的事件，打字机效果实际由前端渲染决定。因此：① TTFT ≈ 全链路耗时（报告口径已如实声明）；② 用"首片 vs 末片到达时间差"**测不出 nginx 缓冲**（本来就没有时间差）。要验缓冲只能直接核 `nginx.conf` 与 `nginx -t`。
- **PowerShell 里给 Python 传含数字的路径会踩转义**：`"...\2026..."` 中的 `\2` 被当转义（实测把 `\20260912-...` 变成了 `\x8260912-...`）。把路径作为**独立参数**传（`& $PY script.py $path` 里读 `sys.argv[1]`），别内插进 here-string。
- **`eval_common.http_json` 传了 payload 就强制用 POST** —— 想发 DELETE 必须显式传 `method="DELETE"`，否则会把 POST 打到 DELETE 路由上，后端兜底成一句含糊的「系统繁忙，请稍后重试」，从错误信息完全看不出是方法错了（实测删知识片段时 5/5 全失败，查了一轮才发现）。
- **改配置/代码后只 `docker compose up -d` 不 `--build`，容器跑的还是旧镜像**。实测：改了 `application.yml`（新增 `rag.demo.*`）后只 `up -d`，容器里的应用**完全没有新配置**，`@Value` 取了代码里的兜底默认值 → 行为看起来"莫名其妙没生效"。排查了一轮才发现是镜像没重建。**改代码或配置一律 `docker compose --profile full up -d --build`。**
- **改了演示账号口令，别忘了同步评估工具的默认值**：`run_eval.py --retrieval` 要 ADMIN 登录，它的 `--admin-pass` 默认值写死在脚本里 → 改完口令后四组检索评估会**静默登录失败**（摘要直接不打印）。已改为读环境变量 `EVAL_ADMIN_PASSWORD`。
- **删知识片段只能走 `DELETE /api/knowledge/chunk/{id}`**（`KnowledgeServiceImpl.deleteChunk` 会同步 `removeVector`）。直连 MySQL `DELETE` 行会留下**幽灵向量**：检索仍能命中一个库里已不存在的 `chunk_id`，界面上完全看不出来。`demo_cleanup.ps1` 与 `find_duplicates.py` 都是走接口删。

## 待办

无

## 构建校验

- 后端：`mvn clean compile -DskipTests`（需 MySQL / PostgreSQL / Redis 已启动并配置 `DEEPSEEK_API_KEY`）
- 前端：`cd frontend && npm run build`
- 单元测试：`mvn test -Dtest=InjectionGuardTest,IntentServiceTest`（16 用例，纯 JUnit 无 Spring 上下文）

## 验证与评估工具链（改完代码先跑这个）

```powershell
pwsh tools\verify_all.ps1              # 一条命令跑完 6 项检查 + PASS/FAIL 汇总表
pwsh tools\verify_all.ps1 -SkipDemo    # 不动知识库
pwsh tools\verify_all.ps1 -SkipBackend # 只跑不需后端的单元检查
```

覆盖：防护规则单元测试 / 行内引用渲染回归 / **缓存旁路探针** / 跑分红线断言 / 逐字保真 / 注入攻防 30 样本。
**这套断言做过负向验证**（把阈值抬到不可能达标的值，确认它真的会 FAIL）—— 永远绿的验证脚本等于没有。

评估相关脚本都在 `tools/eval/`，用法与口径见其 `README.md`；压测在 `tools/loadtest/`。
改动检索/生成/防护后，**必须重跑** `verify_all.ps1` 并把结果记进 `docs/评估报告.md`。

## 知识数据流水线（采集 → 切块 → 导入）

0. **日常入口（推荐）**：`tools/kb_oneclick.ps1` 一键跑通 采集→切块→(可选)小标题→后台导入→修复 vector_status=2（`-DryRun` 预览不导入；默认只导 `-Section` 单栏目防重导）。运营方法论/命令/坑见 **`docs/知识库运营手册.md`**。本小节 1~5 是它背后的单步工具与手工流程。

1. **官网采集**：`tools/web-collect/collect_ysu.py`（纯 Python 标准库）。按 `config.json` 精抓燕大官网公开文章（TRS 的 `v_news_content`/`vsb_content*` 正文容器、自动拼多页），限速 ~1s、跳过微信外链；输出 Markdown 到 `out/<栏目>/` + 来源清单 manifest。**本机 `python` 是商店占位符**，用全路径 `D:\Program\Python\Anaconda3\envs\myenv\python.exe`。栏目文件夹名在 `categories.json` 映射到分类ID（招生政策=1/教务服务=2/学工服务=3）。
2. **切块**：`tools/kb-import/kb_csv_builder.ps1`（PowerShell 7）按 `#` 标题/空行段落聚成 150~500 字一条，输出列 `标题,分类ID,内容,关键词,来源链接`、UTF-8 BOM。跨文件内容去重默认开（`-NoDedup` 关）；单段超 MaxChars 只告警不截断。**来源链接列**：脚本自动检测输入目录下的 `manifest_*.csv`（或用 `-Manifest` 显式指定）按「栏目/文件名」回填——这是热门 Top10 按 `source_url` 归并同一源文的数据基础，别省。
3. **（可选）加小标题**：`tools/kb-import/add_subheadings.py`（纯标准库，需 `DEEPSEEK_API_KEY`）读上一步 CSV + manifest，对每条内容调 DeepSeek 生成 6~14 字小标题，把标题改写成「小标题——源标题」，并单独输出第 6 列 `源标题`（干净的源文标题）。参数：`--csv 上一步.csv --manifest manifest_*.csv --output out.csv [--min-chars 20]`；`--min-chars` 丢弃过短噪声片段（如页脚「终 审 | 杨永涛」）。小标题按内容 MD5 缓存（`--cache`），重复运行不重复计费。手写片段（无来源链接）不改标题、`源标题` 留空。
4. **导入**：`POST /api/knowledge/chunk/import`（ADMIN，multipart `file`）→ `{"success":N,"fail":0}`；随后查 `GET /api/knowledge/chunk/page` 确认 **vectorStatus=1**（2=embedding 失败需重索引）。import 成功 ≠ 向量化成功。导入只增不删不去重，重导前先删旧（`DELETE /api/knowledge/chunk/{id}`，会同步删向量）。
5. 试点库已含：2026 考生问答、迎新入学须知（校园网/邮箱/一卡通/今日校园）、2026 本科新生入学须知。图片式排版文章会残留 `01/Q/A` 类装饰短段，属可接受噪声（页脚「终审」类用 `add_subheadings.py --min-chars` 可丢）。
6. **问答型扩充（学校现成机器人/人工 FAQ）**：`tools/qa-from-bot/qa_to_kb.py`（纯标准库）把「栏目,问题,答案,来源链接」清单转成按栏目分好的 Markdown + `manifest_faq_*.csv`，产物直接喂 `kb_oneclick.ps1`。两模式：`template`（生成/预填待填写清单，`--prefill` 读纯问题列表，行内制表符指定栏目）与 `build`（清单→md+manifest，空答案行自动跳过、同栏目问题去重）。清单未填栏目归「通用」文件夹（无 categories.json 键→分类0）；答案须回官网核对原文再入库。
